#include "main.h"
#include <stdio.h>
/**
 * _puts_recursion - Prints a string followed by a new line
 * @s: string
 *
 * Return: On success 1.
 */
void _puts_recursion(char *s)
{
	puts(s);
	_putchar('\n');
}
